export type EmployeeStatus='IDLE'|'THINKING'|'WORKING'|'WAITING'|'BLOCKED'|'COMPLETED'|'ERROR'|'PAUSED'|'STALLED';
export type TaskStatus='BACKLOG'|'ASSIGNED'|'WORKING'|'WAITING'|'BLOCKED'|'REVIEW'|'COMPLETED'|'FAILED'|'CANCELLED';
export interface EmployeeRecord{ id:string; name:string; role:string; managerId:string|null; model:string; capabilities:string[]; permissions:string[]; status:EmployeeStatus; currentTaskId:string|null; lastHeartbeat:string|null; lastCompletedTaskId:string|null; createdAt:string; updatedAt:string; health:'HEALTHY'|'STALE'|'ERROR'; spendUsd:number; outputs:number; }
export interface TaskRecord{ id:string; goal:string; assignedBy:string; assignedTo:string; parentTaskId:string|null; priority:'LOW'|'MEDIUM'|'HIGH'|'CRITICAL'; status:TaskStatus; progress:number; createdAt:string; startedAt:string|null; updatedAt:string; completedAt:string|null; blockers:string[]; approvals:string[]; result:any|null; }
export interface RunRecord{ id:string; employeeId:string; taskId:string; trigger:string; status:'RUNNING'|'COMPLETED'|'FAILED'|'CANCELLED'|'STALLED'; startedAt:string; endedAt:string|null; heartbeatAt:string; durationMs:number|null; retryCount:number; toolCalls:number; costUsd:number; model:string; }
export interface WorkEvent{ id:string; runId:string; employeeId:string; taskId:string; at:string; type:'STARTED'|'PROGRESS'|'TOOL'|'HANDOFF'|'WAITING'|'BLOCKED'|'ERROR'|'COMPLETED'|'HEARTBEAT'; message:string; metadata?:Record<string,any>; }
