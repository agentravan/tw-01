{
  "name": "tw-01",
  "version": "0.1.0",
  "private": true,
  "description": "Free-first Team Work AI Employee",
  "type": "module",
  "scripts": {
    "dev": "tsx api/server.ts",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "start": "tsx api/server.ts",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^7.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
