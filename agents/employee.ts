import { CRM } from '../crm/crm.js'; import { Memory } from '../memory/memory.js'; import { SkillManager } from '../skills/manager.js'; import { Safety, Audit } from '../tools/safety.js'; import { JsonStore } from '../memory/store.js';
export function plan(command:string){const c=command.toLowerCase();if(c.includes('hot lead'))return['list_hot_leads'];if(c.includes('pause'))return['pause_outreach'];if(c.includes('follow up'))return['list_followups'];if(c.includes('meeting brief'))return['meeting_brief'];return['research_and_plan'];}
export class AIEmployee {crm=new CRM();memory=new Memory();skills=new SkillManager();safety=new Safety();audit=new Audit();constructor(private store=new JsonStore()){} async initialize(){await this.skills.seed()} async command(command:string){const steps=plan(command);await this.audit.record({agent:'ai-brain',task:command,tool:'planner',result:{steps},retry:0,status:'WORKING'});if(steps[0]==='pause_outreach'){await this.safety.setPaused(true);return{status:'WORKING',message:'Outreach paused'}}if(steps[0]==='list_hot_leads')return{status:'WORKING',leads:await this.crm.list('HOT')};if(steps[0]==='list_followups')return{status:'WORKING',leads:(await this.crm.list()).filter(x=>x.status==='FOLLOW_UP')};return{status:'WORKING',steps,message:'Plan created; external actions require approval'}} async dashboard(){const s=await this.store.load();const leads=s.leads;return{totalLeads:leads.length,qualifiedLeads:leads.filter(x=>['QUALIFIED','HOT'].includes(x.status)).length,hotLeads:leads.filter(x=>x.status==='HOT').length,replies:leads.filter(x=>x.status==='REPLIED').length,meetings:leads.filter(x=>x.status==='MEETING_BOOKED').length,proposals:leads.filter(x=>x.status==='PROPOSAL').length,wonClients:leads.filter(x=>x.status==='WON').length,followupsDue:leads.filter(x=>x.status==='FOLLOW_UP').length,paused:s.settings.paused,emergencyStop:s.settings.emergencyStop}}}
